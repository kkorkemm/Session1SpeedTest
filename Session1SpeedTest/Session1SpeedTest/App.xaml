<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Session1SpeedTest.App">
    <Application.Resources>

        <!-- Style Guide -->
        
        <Color x:Key="RedColor">#e2213a</Color>
        <Color x:Key="BlueColor">#005cb9</Color>

        <Style TargetType="Button" x:Key="BaseButton">
            <Setter Property="TextColor" Value="White"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource BaseButton}" x:Key="RedButton">
            <Setter Property="BackgroundColor" Value="{StaticResource RedColor}"/>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource BaseButton}" x:Key="BlueButton">
            <Setter Property="BackgroundColor" Value="{StaticResource BlueColor}"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="Helvetica"/>
        </Style>

    </Application.Resources>
</Application>